# Build-time env for Vite / Docker frontend build
# Never put secrets here that must stay server-only.

# Absolute API origin in production (no trailing slash)
VITE_API_URL=https://api.example.com

# Optional
VITE_SENTRY_DSN=
VITE_BOT_USERNAME=fil_fit_bot
