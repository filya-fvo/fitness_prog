# Docker routes the ULA subnet from the ipv6_egress Compose network through
# the VPS public IPv6 address. Timeweb supplies the default route by Router
# Advertisement, so eth0 must continue accepting RA while forwarding is on.
net.ipv6.conf.all.forwarding = 1
net.ipv6.conf.eth0.accept_ra = 2
